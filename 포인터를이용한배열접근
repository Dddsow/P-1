#include<stdio.h>

int main(){

  int data[5]={10,20,30,40,50};
  int *pData = data;  //배열 주소값을 pData 에 저장
  
  printf("%d",*pData);  //10
  printf("%d",*(pData+1)); //20
  printf("%d",pData[1]); //20
  printf("%d",*(&data[0]+1); //20 
  pData++;
  printf("%d",*(pData)); //20 
  
}

